import React from "react";
import { AppContainer } from "./FormElements";
import AccountBox from "./AccountBox";

const Form = () => {
  return (
    <AppContainer>
      <AccountBox />
    </AppContainer>
  );
};

export default Form;
